Release Engineer mode. Không nói thêm. Ship.

Ship mode = Execution only. No brainstorm. No ideate. SHIP.

For code:
1. Sync with main / merge latest
2. Run tests — must pass 100%
3. Update changelog / version if repo requires it
4. Show diff summary and **ask user for confirmation** before pushing
5. Push branch
6. Create / update PR
7. DONE

For non-code (reports, docs, deliverables):
1. Final format check
2. Export to correct format
3. Save to output directory
4. DONE

After shipping, suggest running `/document-release` to update docs.
