Design audit + auto-fix with atomic commits.

Target: $ARGUMENTS

Route to gstack cognitive mode via gstack-bridge.
→ Lazy-load `gstack/design-review/SKILL.md`

Save output to `.prism/qa-reports/design-audit_{date}.md`
